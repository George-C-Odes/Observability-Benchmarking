<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.georgecodes</groupId>
  <artifactId>orchestrator</artifactId>
  <version>1.0.0</version>
  <name>Orchestrator</name>

  <properties>
    <argLine/>
    <compiler-plugin.version>3.15.0</compiler-plugin.version>
    <exec-maven-plugin.version>3.6.3</exec-maven-plugin.version>
    <findsecbugs.version>1.14.0</findsecbugs.version>
    <google-java-format.version>1.35.0</google-java-format.version>
    <jacoco.skip>false</jacoco.skip>
    <jacoco.version>0.8.15</jacoco.version>
    <maven.compiler.release>25</maven.compiler.release>
    <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
    <maven-pmd-plugin.version>3.28.0</maven-pmd-plugin.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <pmd.cpd.minimumTokens>120</pmd.cpd.minimumTokens>
    <pmd.failure.priority>3</pmd.failure.priority>
    <pmd.include.tests>false</pmd.include.tests>
    <pmd.ruleset.path>quality/pmd-rules.xml</pmd.ruleset.path>
    <pmd.version>7.26.0</pmd.version>
    <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
    <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
    <quarkus.platform.version>3.37.0</quarkus.platform.version>
    <quality.profile.id>quality</quality.profile.id>
    <spotbugs.exclude.filter.file>quality/spotbugs-exclude.xml</spotbugs.exclude.filter.file>
    <spotbugs-maven-plugin.version>4.10.2.0</spotbugs-maven-plugin.version>
    <spotbugs.max.heap>1024</spotbugs.max.heap>
    <spotless-maven-plugin.version>3.8.0</spotless-maven-plugin.version>
    <surefire-plugin.version>3.5.6</surefire-plugin.version>
    <test.jvm.additionalArgs>--add-opens java.base/java.lang=ALL-UNNAMED</test.jvm.additionalArgs>
    <lombok.version>1.18.46</lombok.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-config-yaml</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-validator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-openapi</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-health</artifactId>
    </dependency>

    <!-- Underlying Vert.x client + Mutiny wrappers (BOM-managed versions) -->
    <dependency>
      <groupId>io.vertx</groupId>
      <artifactId>vertx-web-client</artifactId>
    </dependency>
    <dependency>
      <groupId>io.smallrye.reactive</groupId>
      <artifactId>smallrye-mutiny-vertx-web-client</artifactId>
    </dependency>

    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>${lombok.version}</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Compiler plugin with annotation processors -->
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${compiler-plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <parameters>true</parameters>
          <annotationProcessorPaths>
            <!-- Lombok annotation processor -->
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

      <plugin>
        <groupId>io.quarkus</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <version>${quarkus.platform.version}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>generate-code</goal>
              <goal>generate-code-tests</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>${surefire-plugin.version}</version>
        <configuration>
          <argLine>@{argLine} ${test.jvm.additionalArgs}</argLine>
          <systemPropertyVariables>
            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
          </systemPropertyVariables>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire-plugin.version}</version>
        <configuration>
          <argLine>@{argLine} ${test.jvm.additionalArgs}</argLine>
          <systemPropertyVariables>
            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <!-- JaCoCo code coverage — report + advisory threshold check.
           haltOnFailure=false: violations are logged but do not fail
           the build locally or in CI. CI evaluates thresholds
           independently in the GitHub Step Summary. -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <configuration>
          <skip>${jacoco.skip}</skip>
        </configuration>
        <executions>
          <execution>
            <id>default-prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <exclClassLoaders>*QuarkusClassLoader</exclClassLoaders>
              <destFile>${project.build.directory}/jacoco-quarkus.exec</destFile>
              <append>true</append>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <goals>
              <goal>report</goal>
            </goals>
            <phase>verify</phase>
            <configuration>
              <dataFile>${project.build.directory}/jacoco-quarkus.exec</dataFile>
            </configuration>
          </execution>
          <execution>
            <id>check</id>
            <goals>
              <goal>check</goal>
            </goals>
            <phase>verify</phase>
            <configuration>
              <dataFile>${project.build.directory}/jacoco-quarkus.exec</dataFile>
              <haltOnFailure>false</haltOnFailure>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.90</minimum>
                    </limit>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.90</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>${quality.profile.id}</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>${maven-javadoc-plugin.version}</version>
            <configuration>
              <source>${maven.compiler.release}</source>
              <encoding>${project.build.sourceEncoding}</encoding>
              <docencoding>${project.reporting.outputEncoding}</docencoding>
              <charset>${project.build.sourceEncoding}</charset>
              <notimestamp>true</notimestamp>
              <quiet>true</quiet>
              <doclint>none</doclint>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>${exec-maven-plugin.version}</version>
            <executions>
              <execution>
                <id>compact-constructor-javadoc-param-check</id>
                <goals>
                  <goal>exec</goal>
                </goals>
                <phase>validate</phase>
                <configuration>
                  <executable>${java.home}${file.separator}bin${file.separator}java</executable>
                  <arguments>
                    <argument>${project.basedir}${file.separator}quality${file.separator}CompactConstructorJavadocParamCheck.java</argument>
                    <argument>${project.basedir}${file.separator}src${file.separator}main${file.separator}java</argument>
                  </arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>com.diffplug.spotless</groupId>
            <artifactId>spotless-maven-plugin</artifactId>
            <version>${spotless-maven-plugin.version}</version>
            <configuration>
              <lineEndings>GIT_ATTRIBUTES</lineEndings>
              <java>
                <includes>
                  <include>quality/**/*.java</include>
                  <include>src/main/java/**/*.java</include>
                  <include>src/test/java/**/*.java</include>
                </includes>
                <importOrder/>
                <removeUnusedImports/>
                <formatAnnotations/>
                <googleJavaFormat>
                  <version>${google-java-format.version}</version>
                  <style>GOOGLE</style>
                </googleJavaFormat>
                <trimTrailingWhitespace/>
                <endWithNewline/>
              </java>
              <pom>
                <sortPom/>
              </pom>
              <formats>
                <format>
                  <includes>
                    <include>.gitattributes</include>
                    <include>.dockerignore</include>
                    <include>Dockerfile</include>
                    <include>README.md</include>
                    <include>quality/*.properties</include>
                    <include>quality/*.xml</include>
                    <include>src/main/resources/**/*.properties</include>
                    <include>src/main/resources/**/*.yaml</include>
                    <include>src/main/resources/**/*.yml</include>
                    <include>src/test/resources/**/*.properties</include>
                    <include>src/test/resources/**/*.yaml</include>
                    <include>src/test/resources/**/*.yml</include>
                  </includes>
                  <trimTrailingWhitespace/>
                  <endWithNewline/>
                </format>
              </formats>
            </configuration>
            <executions>
              <execution>
                <id>spotless-check</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>validate</phase>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-pmd-plugin</artifactId>
            <version>${maven-pmd-plugin.version}</version>
            <configuration>
              <targetJdk>${maven.compiler.release}</targetJdk>
              <includeTests>${pmd.include.tests}</includeTests>
              <failOnViolation>true</failOnViolation>
              <failurePriority>${pmd.failure.priority}</failurePriority>
              <maxAllowedViolations>0</maxAllowedViolations>
              <verbose>true</verbose>
              <printFailingErrors>true</printFailingErrors>
              <linkXRef>false</linkXRef>
              <minimumTokens>${pmd.cpd.minimumTokens}</minimumTokens>
              <rulesets>
                <ruleset>${pmd.ruleset.path}</ruleset>
              </rulesets>
              <excludeRoots>
                <excludeRoot>${project.build.directory}/generated-sources</excludeRoot>
                <excludeRoot>${project.build.directory}/generated-test-sources</excludeRoot>
              </excludeRoots>
            </configuration>
            <dependencies>
              <dependency>
                <groupId>net.sourceforge.pmd</groupId>
                <artifactId>pmd-core</artifactId>
                <version>${pmd.version}</version>
              </dependency>
              <dependency>
                <groupId>net.sourceforge.pmd</groupId>
                <artifactId>pmd-java</artifactId>
                <version>${pmd.version}</version>
              </dependency>
            </dependencies>
            <executions>
              <execution>
                <id>pmd-check</id>
                <goals>
                  <goal>check</goal>
                  <goal>cpd-check</goal>
                </goals>
                <phase>validate</phase>
              </execution>
              <execution>
                <id>pmd-fqcn-check-tests</id>
                <goals>
                  <goal>pmd</goal>
                  <goal>check</goal>
                </goals>
                <phase>validate</phase>
                <configuration>
                  <targetJdk>${maven.compiler.release}</targetJdk>
                  <includeTests>true</includeTests>
                  <targetDirectory>${project.build.directory}/pmd-fqcn-tests</targetDirectory>
                  <failOnViolation>true</failOnViolation>
                  <failurePriority>${pmd.failure.priority}</failurePriority>
                  <maxAllowedViolations>0</maxAllowedViolations>
                  <verbose>true</verbose>
                  <printFailingErrors>true</printFailingErrors>
                  <linkXRef>false</linkXRef>
                  <rulesets>
                    <ruleset>quality/pmd-fqcn-rules.xml</ruleset>
                  </rulesets>
                  <excludeRoots>
                    <excludeRoot>${project.build.directory}/generated-sources</excludeRoot>
                    <excludeRoot>${project.build.directory}/generated-test-sources</excludeRoot>
                  </excludeRoots>
                </configuration>
              </execution>
              <execution>
                <id>pmd-test-maintainability-check</id>
                <goals>
                  <goal>pmd</goal>
                  <goal>check</goal>
                </goals>
                <phase>validate</phase>
                <configuration>
                  <targetJdk>${maven.compiler.release}</targetJdk>
                  <includeTests>true</includeTests>
                  <targetDirectory>${project.build.directory}/pmd-test-maintainability</targetDirectory>
                  <failOnViolation>true</failOnViolation>
                  <failurePriority>${pmd.failure.priority}</failurePriority>
                  <maxAllowedViolations>0</maxAllowedViolations>
                  <verbose>true</verbose>
                  <printFailingErrors>true</printFailingErrors>
                  <linkXRef>false</linkXRef>
                  <rulesets>
                    <ruleset>quality/pmd-test-rules.xml</ruleset>
                  </rulesets>
                  <excludeRoots>
                    <excludeRoot>${project.build.directory}/generated-sources</excludeRoot>
                    <excludeRoot>${project.build.directory}/generated-test-sources</excludeRoot>
                  </excludeRoots>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <version>${spotbugs-maven-plugin.version}</version>
            <configuration>
              <effort>Max</effort>
              <threshold>Medium</threshold>
              <failOnError>true</failOnError>
              <includeTests>false</includeTests>
              <fork>true</fork>
              <maxHeap>${spotbugs.max.heap}</maxHeap>
              <xmlOutput>true</xmlOutput>
              <outputEncoding>${project.reporting.outputEncoding}</outputEncoding>
              <excludeFilterFile>${spotbugs.exclude.filter.file}</excludeFilterFile>
              <plugins>
                <plugin>
                  <groupId>com.h3xstream.findsecbugs</groupId>
                  <artifactId>findsecbugs-plugin</artifactId>
                  <version>${findsecbugs.version}</version>
                </plugin>
              </plugins>
            </configuration>
            <executions>
              <execution>
                <id>spotbugs-check</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
