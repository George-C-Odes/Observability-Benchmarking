<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       bean-discovery-mode="annotated"
       version="4.0">
    <!--
      In the shaded JVM jar, dependency classes live in the same bean archive as
      application classes. Let Helidon Telemetry's CDI extension register its
      producer beans and avoid discovering the same producer classes by scanning.
    -->
    <scan>
        <exclude name="io.helidon.microprofile.telemetry.**"/>
    </scan>
</beans>
