<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       bean-discovery-mode="annotated"
       version="4.0">
    <!--
      The tracing exclusion prevents WELD-001409 "Ambiguous dependencies for
      type Tracer": Helidon's MpTracingCdiExtension registers TracerProducer
      programmatically and bean discovery would also discover
      the same class through scanning.
    -->
    <scan>
        <exclude name="io.helidon.microprofile.tracing.**"/>
    </scan>
</beans>