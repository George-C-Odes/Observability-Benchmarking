<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       bean-discovery-mode="annotated"
       version="4.0">
    <!--
      Exclusions:
        1. graalvm package – contains GraalVM Feature and @Substitute classes
           that implement org.graalvm.nativeimage.hosted.Feature (hosted-only
           interface). Without this exclusion with bean-discovery-mode="all",
           Jersey/CDI scanning would pick up these classes.
        2. helidon telemetry – lets Helidon Telemetry's CDI extension register
           producer beans and avoids discovering the same producer classes by
           scanning when resources are overlaid for the native image.
    -->
    <scan>
        <exclude name="io.github.georgecodes.benchmarking.helidon.mp.graalvm.**"/>
        <exclude name="io.helidon.microprofile.telemetry.**"/>
    </scan>
</beans>
