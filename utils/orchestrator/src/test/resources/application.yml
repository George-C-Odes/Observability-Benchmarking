# For tests, replace the default CommandRunner bean with a fake one.
quarkus:
  arc:
    selected-alternatives:
      - io.github.georgecodes.benchmarking.orchestrator.application.job.FakeCommandRunner